"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.chunkBytes = void 0;
function chunkBytes(data, maxSize) {
    const chunks = [];
    let currentIndex = 0;
    while (currentIndex !== data.length) {
        const chunkSize = Math.min(maxSize, data.length - currentIndex);
        chunks.push(data.slice(currentIndex, currentIndex + chunkSize));
        currentIndex += chunkSize;
    }
    return chunks;
}
exports.chunkBytes = chunkBytes;
//# sourceMappingURL=xyo-output-stream.js.map