import { IWifiManager, IConnect, IStatus, INetwork } from '../@types';
import { Subscribe } from '@xyo-network/utils';
export default class NodeWifiManager extends Subscribe<IStatus> implements IWifiManager {
    statusInterval: number;
    private logger;
    private wifi;
    private intervalId;
    private updating;
    constructor();
    getStatus(): Promise<IStatus>;
    getNetworks(): Promise<INetwork[]>;
    disconnect(): Promise<boolean>;
    connect({ ssid, password }: IConnect): Promise<undefined>;
    scan(): Promise<IStatus[]>;
    subscribe(fn: (d: IStatus) => void): () => void;
    private startStatusListener;
    private stopStatusListener;
    private updateStatus;
}
//# sourceMappingURL=index.d.ts.map