"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const debug_1 = __importDefault(require("debug"));
const logger = debug_1.default("bleno");
exports.default = (name, services) => {
    const bleno = require("bleno");
    bleno.on("stateChange", (state) => {
        if (state === "poweredOn") {
            logger("Starting broadcast...");
            bleno.startAdvertising(name, services.map(({ uuid }) => uuid));
        }
        else {
            logger("Stopping broadcast...");
            bleno.stopAdvertising();
        }
    });
    bleno.on("advertisingStart", (err) => {
        if (err)
            return logger(err);
        bleno.setServices(services, (error) => {
            if (error)
                return logger(error);
            logger("Services configured");
        });
    });
};
//# sourceMappingURL=startBleServices.js.map