import { Subscribe } from '@xyo-network/utils';
export default class WriteJSONCharacteristic<T> extends Subscribe<T> {
    name: string;
    uuid: string;
    characteristic: any;
    private logger;
    constructor(name: string, uuid: string);
    handleWrite: (body: string) => void;
}
//# sourceMappingURL=json.d.ts.map