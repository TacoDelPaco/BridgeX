"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const create_1 = require("./create");
const utils_1 = require("@xyo-network/utils");
const debug_1 = __importDefault(require("debug"));
class ReadStringCharacteristic extends utils_1.Subscribe {
    constructor(name, uuid) {
        super();
        this.name = name;
        this.uuid = uuid;
        this.logger = debug_1.default('bleno:notify:json');
        this.characteristic = create_1.createNotifyCharacteristic({ name, uuid }, (fn) => {
            const timer = setTimeout(() => fn(JSON.stringify(this.getCurrentValue())), 1000);
            const unsub = this.subscribe(data => fn(JSON.stringify(data)));
            return () => {
                clearTimeout(timer);
                unsub();
            };
        }, this.logger);
    }
}
exports.default = ReadStringCharacteristic;
//# sourceMappingURL=json.js.map