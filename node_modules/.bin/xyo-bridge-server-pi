#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@xyo-network/pi-bridge-server/bin/start-pi.js" "$@"
else 
  exec node  "$basedir/../@xyo-network/pi-bridge-server/bin/start-pi.js" "$@"
fi
